//private -> accessable with in the class only
//default -> with in the curr package only
//protected -> curr package + with in subclass in any other package also (not the objects of those subclass)
//public -> accessable from any where